<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nikola.nbateamwiki">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity
            android:name=".views.MainTeamPage.MainTeamPageActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.TeamInfoPage.TeamInfoActivity" />
        <activity android:name=".views.Gallery.GalleryActivity" />
        <activity android:name=".views.Gallery.FullImageActivity" />
        <activity android:name=".views.FullRoster.FullRosterActivity" />
        <activity android:name=".views.FullRoster.PlayerPageActivity" />
        <activity android:name=".views.StartingFive.StartingFiveActivity"></activity>
    </application>

</manifest>